# Active Execution Plans

Live plans belong in this folder while work is in progress.

Use one file per plan. Name files with a short, stable slug, for example:

```text
brd-semantic-geometry.md
```

When the work is complete, move the plan to `docs/exec-plans/completed/` and add
the result summary to the plan.
