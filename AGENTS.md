# Agent Instructions

Before changing parser, exporter, conversion, CLI behavior, or generated output, read:

- `docs/versioning_policy.md`

Before adding or editing project documentation, read:

- `docs/documentation_style.md`
